import React, { Dispatch, SetStateAction } from "react";

const SearchMenuMobile = ({
  menuOpen,
  setMenuOpen,
}: {
  menuOpen: number;
  setMenuOpen: Dispatch<SetStateAction<number>>;
}) => {
  return <div>SearchMenuMobile</div>;
};

export default SearchMenuMobile;
