import React from "react";

const FeaturedTag = () => {
  return (
    <span className="  rounded-md bg-[hsla(0,0%,100%,.6)]  px-2 py-1 text-[0.675rem] font-bold uppercase text-zinc-800  backdrop:blur-sm">
      izdvojeno
    </span>
  );
};

export default FeaturedTag;
